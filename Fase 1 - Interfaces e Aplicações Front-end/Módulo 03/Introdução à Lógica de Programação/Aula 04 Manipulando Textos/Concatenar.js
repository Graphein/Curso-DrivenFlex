function concatenar(texto1, texto2) {
    // Escreva aqui o código interno desta função, sem alterar seu nome nem os nomes dos parâmetros
    let concatenar = texto1 + texto2;
    return concatenar;
  }
